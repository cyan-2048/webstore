{
	"name": "webstore",
	"source": "src/index.html",
	"scripts": {
		"start": "rd /s /q dev & parcel --no-source-maps --dist-dir dev --no-cache",
		"build": "rd /s /q docs & parcel build --no-source-maps --dist-dir docs --public-url ./"
	},
	"devDependencies": {
		"parcel": "^2.2.1"
	}
}
