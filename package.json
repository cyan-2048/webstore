{
	"name": "webstore",
	"source": "src/index.html",
	"scripts": {
		"start": "parcel --open --no-source-maps --dist-dir dev",
		"build": "parcel build --no-source-maps --dist-dir docs"
	},
	"devDependencies": {
		"parcel": "^2.2.1"
	}
}
